import { ResourceOptions } from '../../help/type/IResource';

const SpaceRelationResource: ResourceOptions = {
	name: '空间关联',
	value: 'space_relation',
	order: 90,
};

export default SpaceRelationResource;
