import { ResourceOptions } from '../../help/type/IResource';

const WorkflowNodeResource: ResourceOptions = {
	name: '流程与节点',
	value: 'workflow_node',
	order: 60,
};

export default WorkflowNodeResource;
