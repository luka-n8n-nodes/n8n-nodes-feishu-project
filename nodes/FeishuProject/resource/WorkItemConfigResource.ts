import { ResourceOptions } from '../../help/type/IResource';

const WorkItemConfigResource: ResourceOptions = {
	name: '工作项配置',
	value: 'work_item_config',
	order: 40,
};

export default WorkItemConfigResource;
