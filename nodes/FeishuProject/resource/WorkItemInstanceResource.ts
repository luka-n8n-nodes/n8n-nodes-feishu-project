import { ResourceOptions } from '../../help/type/IResource';

const WorkItemInstanceResource: ResourceOptions = {
	name: '工作项实例读写',
	value: 'work_item_instance',
	order: 20,
};

export default WorkItemInstanceResource;
